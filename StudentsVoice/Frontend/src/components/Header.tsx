import React from 'react';
import '../styles/Header.css';

const Header: React.FC = () => (
    <header>
        <h1 className="header-title">Student's Voice</h1>
    </header>
);

export default Header;
